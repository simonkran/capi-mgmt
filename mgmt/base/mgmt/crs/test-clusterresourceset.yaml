apiVersion: addons.cluster.x-k8s.io/v1beta1
kind: ClusterResourceSet
metadata:
  name: test-crs
  namespace: default
spec:
  strategy: ApplyOnce
  clusterSelector:
    matchLabels:
      test: enabled
  resources:
    - name: hcloud-crs
      kind: Secret
